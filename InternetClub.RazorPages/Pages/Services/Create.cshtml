@page
@model InternetClub.RazorPages.Pages.Services.CreateModel

@{
    var isRu = System.Globalization.CultureInfo.CurrentUICulture.TwoLetterISOLanguageName == "ru";
    ViewData["Title"] = isRu ? "Добавление услуги" : "Create service";
}

<h1>@(isRu ? "Добавление услуги" : "Create service")</h1>

<div class="row">
    <div class="col-md-6">
        <form method="post">
            <div asp-validation-summary="ModelOnly" class="text-danger"></div>

            <div class="mb-3">
                <label asp-for="Service.Title" class="form-label"></label>
                <input asp-for="Service.Title" class="form-control" />
                <span asp-validation-for="Service.Title" class="text-danger"></span>
            </div>

            <div class="mb-3">
                <label asp-for="Service.PriceMinutes" class="form-label"></label>
                <input asp-for="Service.PriceMinutes" class="form-control" type="number" step="0.01" min="0.01" inputmode="decimal" />
                <span asp-validation-for="Service.PriceMinutes" class="text-danger"></span>
            </div>

            <button type="submit" class="btn btn-primary">@(isRu ? "Добавить" : "Create")</button>
            <a asp-page="Index" class="btn btn-secondary">@(isRu ? "Назад" : "Back")</a>
        </form>
    </div>
</div>

@section Scripts {
    <partial name="_ValidationScriptsPartial" />
}

